{
  "name": "mouse-fixes",
  "version": "1.0.0",
  "description": "Lightweight Claude Code agent that fixes a GitHub issue and opens a PR",
  "type": "module",
  "files": [
    "dist/"
  ],
  "bin": {
    "mouse-fixes": "./dist/index.js"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "prepare": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.9"
  }
}
